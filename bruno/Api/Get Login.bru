meta {
  name: Get Login
  type: http
  seq: 8
}

post {
  url: {{api_url}}login
  body: json
  auth: none
}

body:json {
  { "email": "clement@gmail.com", "password": "clement" }
}

script:post-response {
  bru.setEnvVar("token", res.body.token);
}
