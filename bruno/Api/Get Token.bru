meta {
  name: Get Token
  type: http
  seq: 8
}

post {
  url: {{api_url}}token
  body: json
  auth: none
}

body:json {
  { "email": "clement@gmail.com", "password": "clement" }
}

script:post-response {
  bru.setEnvVar("token", res.body.token);
}
